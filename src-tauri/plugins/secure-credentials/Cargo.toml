[package]
name = "tauri-plugin-paperead-credentials"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-paperead-credentials"

[dependencies]
tauri = "2"
serde = { version = "1", features = ["derive"] }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
