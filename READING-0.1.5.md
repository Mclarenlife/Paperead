# 0.1.5 · 清单 12–17

本轮按原清单顺序补充功能，用户已明确排除 WebDAV，以 Windows 安装包测试交付。旧数据库原地升级至 schema 3，不替换用户资料。备份格式 4 可读取旧格式 1、2、3。

## 使用入口

12. 阅读器 → 导出：直接保存 PDF；Word 支持表格、引用、图片、列表与常见公式的原生 OMML。导出文件名包含原文／译文／重排、版本名称与标识。Markdown 和 HTML 内嵌本地图片。
13. 偏好设置 → 数据与备份：配置应用运行时的自动备份间隔、保留数量、提醒天数；本机快照可预览、导出、删除和恢复。导入备份先列出新增／覆盖记录，确认后先创建恢复前快照；保护快照保存失败时不写入资料库。
14. 文献库 → 引用导入 / 导出：BibTeX / RIS 导入预览及导出，重复 DOI 跳过。引用记录可以附加 PDF。文献信息 → DOI 补全：先查看 Crossref 预览，应用到编辑表单，再保存。PDF 创建日期不再冒充发表年份，正文摘录不再冒充摘要；元数据默认待核验。
15. 我的笔记：文件夹、逗号分隔标签、筛选、历史版本（最多 30 份）、PNG/JPEG/WebP 图片、筛选结果的 Markdown ZIP 批量导出。自动保存按编辑时段合并历史；恢复历史保留当前正文。独立 Markdown 导出内嵌图片，重新导入时转为本地附件。
16. 阅读器 → AI 助手 → 扫描件与版面识别：离线中英文 OCR 或当前配置的视觉模型，可选页码范围、只识别缺少文本页、保留页面图。已识别页面逐页保存进度，最终是一份连续 Markdown；任务卡可暂停和继续。成功识别的正文供后续翻译使用，PDF 与既有结果版本保持可用。视觉模型按四种协议发送图片，严格限制为转录，不询问用户任务或续写正文。
17. Windows x64 安装包。Android 新增 Keystore 加密凭据实现和原生构建入口；其他平台的环境与验收边界见 [原生构建说明](docs/NATIVE-PLATFORMS.md)。本机未构建 Android/macOS/iOS，不提供不存在的 APK/DMG/IPA。

## 验证

截至本记录：70 项单元测试通过；24 项浏览器流程分别通过（完整回归 23 项通过，发现并修复引用库浏览器 Node 依赖后，该流程独立复测通过）。实际扫描 PDF 的 OCR、断点续识别、视觉请求与失败恢复、DOI 预览、附加 PDF、带图笔记 ZIP、快照预览已验证。

导出的中文 PDF 用 Poppler 渲染并逐页检查。DOCX 不仅解包验证 XML，还使用独立隐藏 Word 实例打开并导出为 PDF，再渲染检查；此验证修复了多余 XML 根包裹和公式尺寸问题。本机没有 LibreOffice，标准 render_docx.py 因缺少 soffice 无法运行，已使用真实 Word 完成替代验证。PDF 与 DOCX 均保留表格、中文、图片及常用分式、根号、上下标、求和、矩阵。

Windows 原生验收 25 项通过：阅读与校对 6 项、API 与凭据 9 项、单实例与草稿保护 5 项、新增 OCR／快照／导出／引用 5 项，JavaScript 错误为 0。真实 30 秒响应体超时、流式取消、密钥跨重启与不同地址切换均已回归。原生 PDF 导出检查直接调用发布包中的生成模块并通过真实文件插件写入；系统“另存为”对话框未自动操作。

安装包 `release/Paperead_0.1.5_x64-setup.exe`：32,579,626 字节，主程序文件版本 0.1.5，SHA-256 `27ae11613334465df86dbd1cb4a18aacd5b98b65776d224d4d4453134791fadc`。NSIS 构建成功；本轮没有覆盖安装到用户现有目录。原生测试使用独立标识 `app.paperead.validation015` 和 WebView2 资料目录，验证结束后已还原生产主程序，哈希与构建时备份一致。真实第三方模型未使用用户密钥调用；协议测试不能代表特定模型的识别或翻译质量。

## 边界

本地 OCR 着重识别普通中英文文字；表格、公式和多栏阅读顺序需校对。视觉解析需要支持图片输入的模型，页面图片会发送给用户配置的服务。保留页面图会增加结果和备份体积。PDF 公式独立成行；DOCX 复杂 TeX 扩展未做全面兼容性承诺。原始 PDF 导出不嵌入资料库批注。

应用关闭期间不执行自动备份。快照按类型分别轮换，避免自动备份挤掉恢复前保护。浏览器快照仍受清理站点数据影响；Windows 快照独立于 WebView 存储。大文献、低内存手机仍需压力测试。
